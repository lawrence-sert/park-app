import { ProductCategories } from './product-categories.model';

describe('ProductCategories', () => {
  it('should create an instance', () => {
    expect(new ProductCategories()).toBeTruthy();
  });
});
